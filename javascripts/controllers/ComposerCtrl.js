"use strict";

app.controller("ComposerCtrl", function($scope, $rootScope){
	// $scope, $rootScope, then "ProgressionFactory" or "ChordFactory" will have to be included once created.
	console.log("ComposerCtrl checking in!");
	//console log is appearing twice as of 12.4.2016 --> make sure functions going forward don't duplicate.

});